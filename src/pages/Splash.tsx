import { Container, Typography } from '@material-ui/core';

function Splash() {
    return (
        <Container>
            <Typography> Welcome! </Typography>
        </Container>
    );
}

export default Splash;